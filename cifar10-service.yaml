apiVersion: v1
kind: Service
metadata:
  name: cifar10-service
spec:
  selector:
    app: cifar10-api
  type: NodePort
  ports:
  - name: http
    protocol: TCP
    port: 80          # ← 集群内部统一暴露为80端口（更标准）
    targetPort: 5000  # 容器内的 Flask 端口
    nodePort: 30080   # 外部访问端口
